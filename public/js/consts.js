var consts = (function(){
  'use strict';
  var consts = {};

  consts.resultInvalid = "invalid url format, url must end with ."
  + " followed by a top level domain (e.g. sanic.ca, google.com,"
  + " wikipedia.org, https://github.com)";

  return consts;
}(window));
